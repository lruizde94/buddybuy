# Mercadona React (demo)

Esqueleto Vite + React con un componente inicial `ShoppingList`.

Para arrancar (desde `react-app`):

```bash
npm install
npm run dev
```

No se han hecho commits ni pushes automáticos. Cuando quieras que suba los cambios, indícamelo.
